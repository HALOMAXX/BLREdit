<Window x:Class="BLREdit.UI.Windows.PlaylistEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BLREdit.UI.Windows"
        xmlns:controls="clr-namespace:BLREdit.UI.Controls"
        xmlns:game="clr-namespace:BLREdit.API.Export"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=game:BLRPlaylist}"
        Title="PlaylistEditorWindow" Height="700" Width="1080" ResizeMode="NoResize">

    <Window.Background>
        <ImageBrush ImageSource="../Resources/Default_2016.png" />
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition  Height="48"/>
            <RowDefinition  Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Stretch" Grid.Row="0">
            <TextBox Text="{Binding Path=Name, Mode=TwoWay}" Height="48" Width="256" PreviewKeyDown="PlaylistName_PreviewKeyDown" PreviewTextInput="PlaylistName_PreviewTextInput" VerticalContentAlignment="Center" HorizontalContentAlignment="Left"  />
        </StackPanel>
        <ListView Name="PlaylistListView" ItemsSource="{Binding Path=Entries}" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:PlaylistEntryEditControl/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Add Playlist Entry" Click="AddPlaylistEntry" Width="256" Height="48" VerticalAlignment="Bottom" HorizontalAlignment="Center" Grid.Row="1"/>
    </Grid>
</Window>
